﻿@{
    ViewData["Title"] = "Edit";
}
<br />

<h5 class="text-info">Edit Role </h5>
<br />
<form asp-action="Edit" method="post" id="role-form" enctype="multipart/form-data">
    <div class="p-4 rounded border">
        <div asp-validation-summary="ModelOnly" class="text-danger">
        </div>
        <h3>@ViewBag.mgs</h3>
        <div class="form-group row mt-2">
            <div class="col-2">
                <label>Role Name</label>
            </div>
            <input type="hidden" value="@ViewBag.id" name="id" />
            <div class="col-5">
                <input id="name" name="name" required="required" value="@ViewBag.name" class="form-control" />
            </div>

        </div>


        <div class="form-group d-flex mt-2">
            <input type="submit" class="btn btn-sm btn-primary" value="Update" />
            <a asp-action="Index" style="margin-left:10px" class="btn btn-sm btn-success">Back to List</a>
        </div>
    </div>
</form>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        $(document).ready(function () {
            $("#user-form").validate({
                rules: {
                    UserName: {
                        required: true,
                    },
                    PasswordHash: {
                        required: true,
                    }
                },
                messages: {
                    UserName: "Enter your userName",
                    PasswordHash: "Enter your password"
                }

            });
        });
    </script>
}
